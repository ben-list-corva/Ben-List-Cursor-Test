export const DEFAULT_SETTINGS = {
  isExampleCheckboxChecked: false,
  showProgress: true,
};

// App metadata
export const APP_NAME = 'Operations Onboarding Training';
export const APP_VERSION = '1.0.0';

// Local storage keys
export const STORAGE_KEYS = {
  QUIZ_PROGRESS: 'corva_training_quizzes',
  MODULE_PROGRESS: 'corva_training_progress',
};
