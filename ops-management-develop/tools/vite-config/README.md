# vite-config

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build vite-config` to build the library.

## Running unit tests

Run `nx test vite-config` to execute the unit tests via [Vitest](https://vitest.dev/).
